{% extends 'base.html.twig' %}

{% block title %}Edit Trick{% endblock %}

{% block body %}
    {% include('shared/_alert.html.twig') %}

<section class="info-trick">
        {{ include('trick/_form.html.twig', {'button_label': 'Modifier'}) }}
        <a href="{{ path('home') }}">back to list</a>
        {% embed "trick/_delete_form.html.twig" %}
                {% set btn = '<button class="btn btn-outline-danger btn-lg">Supprimer</button>' %}
        {% endembed %}
</section>

{% endblock %}

{% block js %}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="{{ asset('js/media.js') }}"></script>
{% endblock %}