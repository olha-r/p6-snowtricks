
<form method="post" action="{{ path('trick_delete', {'slug': trick.slug}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce trick?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trick.slug) }}">
{#    <button type="submit" class="btn btn-lg btn-danger">Supprimer</button>#}
{{ btn|raw }}
</form>
