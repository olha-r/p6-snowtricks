{% if trick.media|length > 0  %}
{#    or trick.videos|length > 0#}
    <div class="carousel">
        {% set count = 1 %}
        <div class="slider-outer">
            <a class="prev"><i class="fas fa-chevron-left"></i></a>

            <div class="slider-inner">
                {% for media in trick.media %}

                    <img src="{{ asset('/uploads/'~ media.name) }}" {% if count == 1 %} class="active"
                         {% endif %}alt="test"/>
                    {% set count = count + 1 %}

                {% endfor %}

{#                {% for video in trick.videos %}#}

{#                    <iframe width="100%" height="100%" src="{{ video.videoUrl }}"#}
{#                            allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture"#}
{#                            allowfullscreen {% if count == 1 %} class="active" {% endif %}></iframe>#}
{#                    {% set count = count + 1 %}#}

{#                {% endfor %}#}

            </div>
            <a class="next"><i class="fas fa-chevron-right"></i></a>
        </div>


    <div class="row mt-5 mb-5">
        <div class="col-lg-12 text-center">
            <button class="btn btn-primary" id="btn-media">Medias</button>
        </div>
    </div>

{% else %}

    <div class="container text-center"><strong>Pas de médias disponible pour ce trick</strong></div>

{% endif %}